rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Entries: owner can read/write, anyone can read shared entries
    match /entries/{entryId} {
      allow read: if request.auth != null
        && request.auth.uid == resource.data.userId;
      allow read: if resource.data.visibility == 'shared';
      allow write: if request.auth != null
        && request.auth.uid == resource.data.userId;
      allow create: if request.auth != null
        && request.auth.uid == request.resource.data.userId;
    }

    // Friends: only the owner can read/write their own friends
    match /friends/{friendId} {
      allow read, write: if request.auth != null
        && request.auth.uid == resource.data.userId;
      allow create: if request.auth != null
        && request.auth.uid == request.resource.data.userId;
    }

    // Invites: owner can create/read, anyone can read by ID
    match /invites/{code} {
      allow read: if true;
      allow create: if request.auth != null
        && request.auth.uid == request.resource.data.userId;
      allow write: if request.auth != null
        && request.auth.uid == resource.data.userId;
    }

    // Push notification subscribers: anyone can write (public family feed viewers),
    // only Cloud Functions (admin SDK) read/delete
    match /pushSubscribers/{tokenId} {
      allow write: if request.resource.data.keys().hasAll(['userId', 'token', 'createdAt'])
        && request.resource.data.userId is string
        && request.resource.data.token is string;
    }
  }
}
